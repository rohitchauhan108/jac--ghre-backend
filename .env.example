PORT=4000
# Production MongoDB connection string (Atlas)
MONGODB_URI=mongodb+srv://developmentrankmantra_db_user:NOci9qqfemtDCT2f@cluster0.l4ht1fb.mongodb.net/?appName=Cluster0

# Frontend (Vercel) + Backend (Render) public URLs
FRONTEND_URL=https://jac-ghre.vercel.app
API_PUBLIC_URL=https://jac-ghre-backend.onrender.com

# Auth (required) â€” any long random string. Generate one with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=30d

# PhonePe payment gateway (required for online payments; COD works without it)
PHONEPE_MERCHANT_ID=
PHONEPE_SALT_KEY=
PHONEPE_SALT_INDEX=1
PHONEPE_BASE_URL=https://api-preprod.phonepe.com/apis/pg-sandbox

# Resend transactional email (required for sign-up OTP / password-reset / order confirmations)
# 1. Add & verify your sending domain at https://resend.com/domains
#    - DKIM:  TXT  resend._domainkey   -> copy value from Resend dashboard
#    - SPF:   TXT  (root / @)          -> v=spf1 include:spf.resend.com ~all
# 2. Create an API key at https://resend.com/api-keys
RESEND_API_KEY=
# Sender must be an address on a domain you verified in Resend.
# Verified domain: dnddevelopment.net
RESEND_FROM_EMAIL="Jac GhrÃ© <accounts@dnddevelopment.net>"

# Inbox that receives all messages submitted via the site contact form.
# Also used as the delivery destination (to:) for contact emails.
CONTACT_INBOX=Ghrebeauty@gmail.com

NODE_ENV=development
